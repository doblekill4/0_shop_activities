# 2026-06-12 工作日志

## 通知模板系统重构 + 功能新增

### 新增两个订阅消息模板
| 用途 | 模板ID | 字段 |
|---|---|---|
| 清洁任务提醒 | `gw8f84WumXoZkBDaMErZ7YVDTna9P8jwosJf0bURSSg` | time3(提醒时间), thing1(清洁地址), thing2(清洁内容) |
| 活动状态变更通知 | `vRCdbLk5V3L1OpnyPm7M5oOUWIBJIZh7jnNi6SFRfwA` | time4(开始时间), thing1(活动名称), thing2(活动描述), phrase3(活动状态), thing7(联系人) |

### 模板使用场景映射
- 定时提醒（开始前/结束后/环节前）→ XrO2... (原模板，不变)
- 流程完成后清洁通知 (timingIndex=4) → gw8f84... 清洁任务提醒
- 活动变更通知 (sendChange) → vRCdbLk... 活动状态变更通知

### 部门主管交集查找逻辑
- `findDepartmentSupervisors(departmentName)` 新增到 notifications 云函数
- 规则：部门成员 ∩ 部门主管权限组
- 匹配方式：permissionGroupId 匹配 + 管理权限匹配 + admin

### 责任人滚轴增加"待分配"
- create/edit 页面选择器中每部门人员末尾追加「待分配」
- 选择后 ownerId=`__pending__`, ownerName='待分配', 保存 ownerDeptName 用于通知主管

### 权限组管理限制（部门主管）
- `permission-groups.js` manageMembers: 非admin用户只能管理自己所属权限组的成员
- 防止A部门主管误调BCD部门成员

### 日期/人数字段解析优化
- 日期：支持 YYYY-MM-DD、年/月/日、YYYY/M/D 三种格式
- 人数：区间 "15-20人" → 取分隔符后方 "20"

### 滚轴店长组排序修复
- create + edit 两处 `_buildDeptUserPicker` 改为显式 splice 店长组到末尾

### 部署清单
- 需重新部署 `notifications` 云函数
- 前端热更新即可生效
