# 2026-07-18

## v1.0.3 改动总结（需提审转正式版）

### 前端改动（需体验版 → 审核 → 正式版）
1. **审核快捷登录** - 已有 Storage 直接复用，不创建重复账号；云函数仅新用户写 name/department
2. **移除详情页 3s 冷却** - loadDetail 去掉节流，确认环节/上传凭证即时刷新
3. **列表底部分页文案** - "上拉加载更多（当前 N 条）" / "已加载 N 条，没有更多了"
4. **发送通知渐隐窗口** - notification-sheet 发送后弹出 result-toast（2s渐隐）
5. **环节确认渐隐提示** - activity-detail 确认后弹出 step-toast
6. **通知错误可复制** - `selectable="true"` 在 notifyLastError 上
7. **43101 友好提示** - "接收方未授权，请让对方重启小程序授权"
8. **管理子页面 loading** - user-manage / notification-config 进入时 showLoading
9. **创建/编辑保存提交 loading** - saveDraft/_doSubmit/saveEdit/submitDraft 加 loading
10. **识别填充不滚顶** - parseInfo 后恢复 scrollTop
11. **业务体现 textarea** - input→textarea 支持换行，maxlength=-1

### 云函数改动（已即时部署）
- **auth**: 审核快捷登录修复、notifyAuthVersion 返回
- **notifications**: notifyEnabled 检查、timingIndex 0 自动取第一个环节负责人

### 版本号
- app.js: 1.0.1 → 1.0.3
- README 更新 v1.0.3 更新日志
- MEMORY.md 版本同步

### 门店群白名单
- 恢复 settings 集合方案，需手动建 settings 集合
- activateStoreGroup 写入 manual_activated 值
- 体验版 wx.getGroupEnterInfo() 返回 null，故用手动激活兜底
- 审核前重置白名单 → 审核员能进 → 审核后王万全再激活
