# 2026-06-14

## WXML 标签闭合错误修复
- `pages/activity-create/activity-create.wxml`: 删除了第54-55行两个多余的 `</view>` 闭合标签。日历 section 的 `<view class="card calendar-section">` 已在第53行正确闭合，多余的标签导致编译器报错 `expect end-tag 'scroll-view', near 'view'`。

## 活动列表 step 摘要加地点
- `pages/activity-list/activity-list.js`: `_formatItem` 中 steps 映射补充 `venue: s.venue || ''`
- `pages/activity-list/activity-list.wxml`: 步骤行加入 `📍{{step.venue}}` 蓝色徽章（`wx:if` 有地点才显示）
- `pages/activity-list/activity-list.wxss`: 新增 `.step-venue` 样式，蓝色底色 `#E3F2FD`，文字 `#1565C0`，与详情页保持一致，最大宽160rpx超出省略

## 新建/编辑活动三合一修复
### 地点选择器默认值
- `activity-create.js` / `activity-edit.js` `addStep()`: `venueIndex` 从 `VENUE_LIST.length - 1`（其他）改为 `0`（零号店1-3层）
- `ownerDeptValue` 从 `[0, 0]` 改为 `[0, -1]`，新建步骤默认"待分配"

### 负责人部门排序
- `activity-create.js` / `activity-edit.js` `_buildDeptUserPicker()`: 当前用户所在部门排到最前（其他按拼音，店长末尾）
- `onStepOwnerColumnChange()`: 切换部门时自动将人员列跳到"待分配"（最后一个），通过 `e.currentTarget.dataset.index` + `setData` 更新 `ownerDeptValue`

### 手动输入地点报错修复
- `activity-create.js` `confirmCustomVenue()`: `this._venueStepIdx` → `this.data._venueStepIdx`（setData 写入的是 data 层），增加 idx 有效性校验

## 甘特图场地使用区重构
- `gantt.wxml`: 新增 `scroll-view scroll-y class="gantt-main-scroll"` 包裹 gantt-body + legend-bar，场地使用区从独立 section 移入 `gantt-scroll-inner` 内部紧跟活动行
- 冻结列新增 `frozen-venue-header` 和 `frozen-venue-row` 显示场地名称，右侧时间网格内 `venue-section-inner` 对齐时间轴
- `gantt.wxss`: 新增 `.gantt-main-scroll { flex:1; height:0 }`，移除旧 `.venue-section` 独立样式，新增冻结列场地标签样式

## 甘特图场地区改为简洁列表
- 移除冻结列场地标签和右侧时间轴对齐的 venue-section-inner（过度设计，无实际价值）
- `gantt.wxml`: 在 gantt-body 下方添加 `.venue-card` 卡片列表，每个场地 `.venue-group` 下按「时间 | 单位 | 环节名」简单排列
- `gantt.wxss`: 新卡片样式，绿头白底，左侧蓝色竖线指示每项
- `gantt.js` `_buildVenueOccupancy()`: 移除 left/width/label 时间轴对齐计算，仅保留原始字段

## 粘贴识别的环节地点预填写
- `activity-create.js`: 新增 `VENUE_ALIASES` 别名映射（食堂→员工餐厅、会议室二→五层会议室二、LED区→三层LED区 等15组）
- 新增 `matchStepVenue(stepName)` 函数：三级匹配策略（别名精确命中 → 子串包含 → 2字bigram重叠度≥4分阈值）
- `parseInfo()` 中所有5种步骤解析模式（时间在前后/单双时间/仅名称）均调用 `matchStepVenue()`，预填 `venue`、`venueIndex`、`ownerDeptValue` 字段
- 示例效果：「参观零号店1-3层」→ 零号店1-3层，「会议室二」→ 五层会议室二，「前往食堂」→ 员工餐厅，「午餐/集合返程」→ 不匹配
