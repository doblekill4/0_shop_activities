# 2026-07-02

## 开发者工具上传体验版 OOM 排查
- 用户反馈上传体验版编译时卡在 `behaviors/formBase.js` 并报 `out of memory`。
- 可能诱因：behavior 文件中残留 ES2017 `padStart` / ES6 `findIndex`，微信小程序 babel 转译/压缩时容易死循环或内存溢出。
- 已修复：
  - `behaviors/formBase.js`：将 `String(i).padStart(2,'0')` 改为手动补零。
  - `behaviors/stepEditor.js`：将 `deptUsers.findIndex(...)` 改为 for 循环。
  - `project.config.json`：临时关闭 `"minified": false`，避免压缩阶段 OOM。
- 后续建议：每次改完 behavior 后先用基础库 2.19 以下真机/上传测试；如仍 OOM，需清理 `%LOCALAPPDATA%\微信web开发者工具` 和 `%APPDATA%\微信web开发者工具` 缓存。
